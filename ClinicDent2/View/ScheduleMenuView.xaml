<UserControl x:Class="ClinicDent2.View.ScheduleMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClinicDent2.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Unloaded="UserControl_Unloaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Calendar x:Name="datePicker" FirstDayOfWeek="Monday" SelectedDate="{Binding SelectedDate}" Grid.Row="0" HorizontalAlignment="Left" Focusable="False" MouseUp="datePicker_MouseUp" SelectedDatesChanged="datePicker_SelectedDatesChanged"/>
        <ScrollViewer x:Name="scrollViewerSchedule" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" CanContentScroll="False" Grid.Row="1" Focusable="True" >
            <StackPanel x:Name="stackPanelDays"/>
        </ScrollViewer>
        
    </Grid>
</UserControl>
